name: wolfram
description: 使用wolfram对公式进行计算，Useful for when you need to calculate difficult math formulas
parameters:
  type: object
  properties:
    query:
      type: string
      description: The formula to be calculated
required:
  - query